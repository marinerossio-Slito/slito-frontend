# URL de base de l'API Slito (back-end Symfony, cf. slito-backend).
# En local, démarrer le back-end avec `symfony server:start` (ou `php -S 127.0.0.1:8000 -t public`)
# puis copier ce fichier en .env.local et ajuster si besoin.
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000
